# AIMO3 Hybrid Learning System - Requirements

# Core dependencies (already installed)
numpy>=1.24.0
json

# Phase 1: Hybrid System (CPU-only)
sentence-transformers>=2.2.0  # Embedding-based RAG
scikit-learn>=1.3.0           # Neural pattern classifier
faiss-cpu>=1.7.0              # Fast similarity search (optional, for large corpora)

# Phase 2: LLM Fine-tuning (GPU required)
# Uncomment when ready for Phase 2
# transformers>=4.35.0
# datasets>=2.14.0
# accelerate>=0.24.0
# peft>=0.6.0                 # Parameter-efficient fine-tuning
# bitsandbytes>=0.41.0        # 8-bit quantization (optional)

# Phase 3: RL Training (GPU required)
# Uncomment when ready for Phase 3
# trl>=0.7.0                  # Transformer Reinforcement Learning
# torch>=2.0.0
# gymnasium>=0.29.0

# Testing
pytest>=7.4.0

# Utilities
tqdm>=4.66.0                  # Progress bars
matplotlib>=3.8.0             # Visualization (optional)
