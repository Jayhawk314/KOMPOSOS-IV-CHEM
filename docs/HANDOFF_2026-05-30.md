# Handoff — 2026-05-30 (start a fresh session here)

Plain-English handoff so a new session (or a new agent) picks up without re-reading
a long transcript. Read this, then `docs/FEATURE_STATUS_AND_ROADMAP.md`.

## Who you're working with
James — a **vibe coder** who built the KOMPOSOS categorical system. Talk in plain
language, not metric jargon (translate ECE/AUROC/etc.). He values **blunt honesty
over polish**, and wants to be **in the loop** — discuss and propose, don't barrel
ahead and edit. When he asks a question, answer it in words first.

## What happened this session (all committed + pushed)
Branch `remediation/compatibility-coverage-2026-05-29` is on GitHub. 6 commits:
- **MOF Designer** — built + validated a "grounded funnel" (chemistry sanity, ≥2
  binding sites, ease-of-synthesis, geometry, novelty) against real synthesized
  linkers (MOFSimplify + CoRE-MOF): ~94% recall on real linkers, good real-vs-junk
  separation. Wired into the page. Honest gap: no wet-lab proof.
- **PFAS Scanner** — now detects PFAS by **chemical structure** (the official OECD
  rule) + resolves names to structures via PubChem, so it catches **novel** PFAS.
  Cleared 25/25 non-PFAS look-alikes; matched the EPA list 99.5%. Fixed a real
  false-positive bug and a broken PubChem call.
- **Crystal Dreamer** — honestly an **idea generator, not a precise predictor**
  (~78% property recovery; can't reinvent exact/unusual chemistries). Improved the
  search so it explores composition ratios properly.
- **Composition Predictor** — its property numbers are **rough** on unfamiliar
  chemistry (~0.5 eV/atom off) and its uncertainty bars were lying (too narrow).
  **Fixed the bars** so they're honest (checked out-of-sample). Point numbers
  untouched.
- **Compatibility Checker** — real physics (not heuristics), 77–100% per domain.
  Its confidence number is a **rough guide near its accuracy ceiling** (~0.10
  error) — fixed the wording to say so rather than chase a tiny gain.
- **UI** — `streamlit_app/validation_status.py` is the **single source of truth**
  for every accuracy/honesty claim; updated to plain language. Never hardcode a
  number in a page.

## What is NOT committed (you decide)
The branch still has **pre-existing uncommitted work from earlier (Codex) sessions**
that this session did NOT create and did NOT vouch for:
- Modified: `battery_bridge/`, `polymer_bridge/`, `composition_engine/parser.py`,
  `composition_engine/structure_predictor.py`, `data/drugs/tier1.db`,
  `data/strategy_weights_with_topos.json`, `docs/AUDIT_CHANGE_LOG.md`,
  `docs/TUNING_LOG.md`, `streamlit_app/material_search.py`.
- Untracked: `G-docs/` (Gemini's design notes — valued, but design not product
  claims), `audit/*.json` reports, Q9/Q10 blind files, `data/cyber/`, log files.
Decide with James whether to commit these (e.g. the polymer fix + G-docs are real
work worth preserving) — but don't commit blind; confirm their state first.

## Next steps (most researcher value first)
1. **PFAS → usable alternatives**: run each suggested replacement through the
   compatibility engine, so you get "PFAS-free AND works in your setup."
2. **Compatibility confidence**: lock in a fresh blind benchmark; calibration is
   near its ceiling so the win is a clean blind number, not more calibration.
3. **Crystal Dreamer point accuracy**: target-aware anchors (pick neighbors near
   the target chemistry) — helps the isolated cases (LCO, LTO, graphite).
4. **Battery "Optimize" mode** in Cell Designer — honest objective only
   (theoretical energy density under compatibility/PFAS constraints).

## Hard rules
- **Q10 is the sealed final exam — never open or tune to it.** No dataset is
  currently "blind"; Q2–Q9 are spent diagnostics.
- Per-domain claims with uncertainty only; no single global accuracy headline.
- Run feature-specific shards, not the whole monolithic pytest tree.

## How to verify quickly
- App: `streamlit run streamlit_app/app.py` → honest banners are under
  "Accuracy & uncertainty" on each page.
- Benchmarks: `python audit/run_pfas_audit.py`, `python audit/run_crystal_recovery.py`,
  `python audit/run_predictor_accuracy.py`, `python -m mof_bridge.benchmark.run`.

## Honest one-liner
Today it's a **research triage + idea toolkit with honest uncertainty** — strongest
at PFAS detection, MOF generation, and compatibility triage; roughest at the
property *numbers*. The path to "decision-grade" runs through **calibration**
(cheap) and **forward-model accuracy** (the hard frontier).
