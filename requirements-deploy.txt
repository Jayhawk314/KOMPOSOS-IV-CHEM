# KOMPOSOS-IV-CHEM - Deployment Requirements
# rdkit enables MOF Designer (page 8); needs a paid instance with >=1GB RAM
# (512MB free tier cannot hold rdkit + streamlit). Verified 2026-08-12: a
# tracked-files-only clone generates MOF linkers end-to-end with rdkit present;
# the seed cache auto-builds in demo mode on first use.
# Still excluded (heavy, only needed by MP Explorer page 6): pymatgen, mp-api
# Python >=3.10

# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
networkx>=3.0

# Data & Storage
aiosqlite>=0.19.0

# HTTP
aiohttp>=3.9.0

# Web API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Web UI
streamlit>=1.30.0

# Chemistry engine (MOF Designer, page 8)
rdkit>=2023.9.1

# PDF report generation
fpdf2>=2.7.0

# Plotting
matplotlib>=3.7.0

# Testing (needed for CI, harmless in deploy)
pytest>=7.0.0
httpx>=0.25.0

# SMT solver (stoichiometric validation in synthesis_planner)
z3-solver>=4.12.0
